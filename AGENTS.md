# 围之象棋 · Agent 开发指南

本文件适用于整个仓库。修改前先阅读相关实现和测试；以下约定描述当前产品行为，用户明确要求调整时，应同步更新代码、测试及文档。

## 项目与技术栈

- 中文名称为 **围之象棋**，英文项目名为 **Xiangqi-Go**，包名为 `xiangqi-go`。
- Node.js 22+，JavaScript ES Modules；浏览器使用原生 HTML、CSS、SVG、Web Audio 和 Web Worker。
- 当前无第三方运行依赖、无构建步骤。优先沿用现有架构，避免为局部功能引入框架或打包工具。
- 联机使用同源 HTTP POST 指令与 SSE 状态推送；服务端是棋局状态和胜负的唯一权威。
- 默认进入主菜单；联机大厅与棋盘为独立界面。练习弹窗可选 PVE 阵营和难度，同时保留本地 PVP。联机 / PVE 己方在下方，PVP 随行棋方翻转。

## 文件职责

| 文件 | 职责 |
| --- | --- |
| `engine.js` | 共享规则、连通棋块、气、提子、胜负、将军检测与 AI 搜索 |
| `engine.test.js` | 规则与 AI 战术回归测试 |
| `lobby.js` | 会话、房间、准备、阵营分配、服务器走子与断线清理 |
| `lobby.test.js` | 房间状态机与 HTTP / SSE 接口测试 |
| `server.js` | HTTP 服务、静态文件白名单、身份校验、限流及 SSE 连接 |
| `online.js` | 浏览器会话、重连、版本过滤与联机指令 |
| `users.js` | 账号注册、登录、密码哈希、游客生成与管理员封禁管控 |
| `users.test.js` | 用户模型、权限限制与管理员管控回归测试 |
| `app.js` | SVG 棋盘、输入、动画、提示、大厅和本地 / 联机状态展示 |
| `ai.js` | 人机计算的 Web Worker 入口 |
| `audio.js` | 本地合成配乐、音效、音量与暂停控制 |
| `index.html` / `style.css` | 页面结构、中文文案和响应式样式 |
| `README.md` / `DEPLOY.md` | 用户说明与部署步骤 |
| `Dockerfile` / `compose.yaml` | 单实例容器部署 |

## 核心规则约定

- 棋盘为 9 列 × 10 行，索引为 `y * 9 + x`。`y=0` 是黑方底线，`y=9` 是红方底线。
- 棋子形如 `{s, t}`：阵营 `r` / `b`；类型 `k` 将帅、`a` 士仕、`e` 象相、`h` 马、`c` 车、`p` 炮、`s` 兵卒、`g` 围棋。空位为 `null`。
- 红方象棋与白围棋同阵营；黑方象棋与黑围棋同阵营。上下左右相邻的己方棋子共享去重后的气，跨河连接，斜向不连接。
- 每回合移动一枚象棋，或在空交叉点落围棋。围棋不能移动；传统象棋的路径、九宫、过河及兵卒方向限制仍适用。
- **炮架不分敌我，也不分象棋或围棋**。炮吃子必须同行或同列且中间恰好一枚棋子；不吃子时路径必须为空。
- 先处理直接吃子，再提掉对方无气棋块。**捕获或围死对方将帅立即获胜，优先于己方自杀检查**；普通着法仍禁止自杀及重复棋盘局面。
- 采用捕获将帅制，不强制应将，保留飞将吃子。将军提示须覆盖直接吃将和围杀威胁；无合法行动判负。
- 规则逻辑集中在 `engine.js`，前端、AI、服务端复用，不另写相互矛盾的判定。
- 修改规则时保留输入棋盘不被原地修改的约定。特别保留炮从 E6 越过敌方 E9 黑子吃 E10 将、落点无气仍获胜的回归用例。

## 交互与 AI

- 不恢复“走象棋 / 落围棋”切换按钮。选中己方象棋后显示合法走位，点击非法位置取消；未选中时空位悬停显示己方围棋虚影，点击落子。
- 红方象棋白色外圈、黑方黑色外圈，与同阵营围棋颜色对应。保留气数、吃子、将军与胜利提示。
- 保留移动动画、键盘操作、触屏适配和减少动态效果支持。页面加载时立即尝试播放配乐；浏览器限制自动播放时，在首次用户交互后恢复。配乐和音效可独立关闭。
- AI 在 Web Worker 中计算。先识别立即获胜及一步丢将威胁，再做有时间预算的搜索；不要通过奖励无意义的围棋数量来评价局面。
- 悔棋、重开、模式切换、重连及新状态到达时，应使过期动画和异步计算结果失效，防止旧结果覆盖新棋局。

## 联机边界

- 默认最多 5 个房间，管理员可设置 1–100 间并持久化，每房 2 人；房间名与游客名由服务端随机生成。降低上限不解散已有房间。
- 两人都连接且准备后，服务端随机分配红黑方并让红方先行。第二人加入待开局房间时，保留第一人已有的准备状态。
- 主动退出立即离房，对局中退出判负；断线暂停，保留座位约 30 秒，超时移出。最后一人离房后删除房间。
- 服务端必须校验身份、成员资格、回合、游戏 ID、步数和完整走法。保留请求去重、版本过滤及重连恢复。
- 联机不能使用本地悔棋或单方重开；重新开局需要双方再次准备。
- 会话令牌保存在 `localStorage`（降级至 `sessionStorage`），作为持久化设备凭据通过 Authorization 头传输。不要将令牌写入 URL、日志、公共状态或仓库。
- 动态玩家 / 房间文本使用 `textContent` 或等效安全方式渲染。新增前端文件时检查 `server.js` 静态白名单及 Docker 文件清单，不公开服务端源码、环境文件或测试文件。

## 开发与验证

在项目根目录运行：

```sh
npm start
npm test
```

- 默认访问 `http://127.0.0.1:5173`，不要用 `file://` 打开页面。
- 如已有服务或正在对局，优先在另一端口验证，避免重启清空用户的联机棋局。PowerShell 示例：`$env:PORT = '5174'; npm start`；POSIX shell 示例：`PORT=5174 npm start`。
- 规则、AI、服务端改动应补充有针对性的回归测试并运行 `npm test`。异步房间测试优先使用注入时钟，避免等待真实断线宽限期。
- 前端交互改动应实际在浏览器验证相关操作；联机改动使用两个独立浏览器上下文，核对双方棋盘、棋谱、回合、刷新恢复及离房清理。复制标签页可能共享身份，不能作为独立玩家测试依据。
- 布局改动检查桌面和窄屏。纯文案或文档改动检查内容与 `git diff --check` 即可，不必重复运行无关测试。
- 报告实际执行的验证，区分自动化测试、浏览器实测和未验证项，不把命令行成功等同于界面正确。

## 部署与提交

- 当前房间保存在单进程内存中，服务重启清空。不要直接扩为 cluster 或多副本；此类变更需配套共享状态与消息同步。
- 保留 `HOST`、`PORT`、`PUBLIC_ORIGIN` 配置能力，前端接口使用同源相对地址。
- SSE 代理需关闭缓冲和缓存并允许长连接。部署行为变化时同步维护 `DEPLOY.md`、Docker 配置及 README。
- 使用 UTF-8，保持改动集中；不要顺带格式化整个仓库或覆盖用户未提交的修改。
- 不提交密钥、环境文件、日志、依赖目录或测试截图。确需版本控制的图片资源应调整 `.gitignore`，当前全局图片忽略规则可能会屏蔽它们。
- 按当前任务范围处理 Git 提交、推送与部署；仅添加文件不代表请求上线。提交信息说明具体行为变更，完成报告简要列出变更、验证和剩余限制。
